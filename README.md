# Student-Record-Management-System
A C-based console application to manage student records using file handling and structured data. Features include adding, displaying, searching, and deleting student records stored in a binary file. The system also auto-generates a CSV export for easy data viewing and backup.
